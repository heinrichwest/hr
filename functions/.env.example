# ============================================================
# FIREBASE CLOUD FUNCTIONS ENVIRONMENT VARIABLES
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# ============================================================

# SMTP Configuration (for local testing only)
# Production credentials are stored in Firebase Extension configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=hr@speccon.co.za
SMTP_PASSWORD=your-smtp-password-here

# Application URL (for email links)
APP_URL=https://hr-system-9dfae.web.app

# Firebase Project ID
FIREBASE_PROJECT_ID=hr-system-9dfae
